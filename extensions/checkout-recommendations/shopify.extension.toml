# Learn more about configuring your checkout UI extension:
# https://shopify.dev/docs/api/checkout-ui-extensions/latest/configuration

# The version of APIs your extension will receive. Learn more:
# https://shopify.dev/docs/api/usage/versioning
api_version = "2026-04"

[[extensions]]
name = "Checkout Recommendations"
handle = "checkout-recommendations"
type = "ui_extension"
uid = "d296a286-e696-6ece-36cc-64f3fd7b258c5b2b48b8"

# Controls where in Shopify your extension will be injected,
# and the file that contains your extension’s source code. Learn more:
# https://shopify.dev/docs/api/checkout-ui-extensions/latest/extension-targets-overview

[[extensions.targeting]]
module = "./src/Checkout.jsx"
target = "purchase.checkout.block.render"

[extensions.capabilities]
# Gives your extension access to directly query Shopify’s storefront API.
# https://shopify.dev/docs/api/checkout-ui-extensions/latest/configuration#api-access
api_access = true

# Gives your extension access to make external network calls, using the
# JavaScript `fetch()` API. Learn more:
# https://shopify.dev/docs/api/checkout-ui-extensions/latest/configuration#network-access
# network_access = true

# Merchant-facing configuration (manual upsells, heading, max products,
# free-shipping thresholds, motivators) is managed in the app's Checkout
# Recommendations admin page and read at runtime from the
# `$app:checkout-recommendations/config` shop metafield. The only extension
# setting is a debug toggle below.

[extensions.settings]

# When enabled, renders a diagnostics panel in checkout showing the loaded app
# config, detected currency/subtotal, and why each motivator did or didn't match.
# Use it to debug configs that aren't appearing. Leave off in production.
[[extensions.settings.fields]]
key = "show_testing_information"
type = "boolean"
name = "Show Testing Information?"
