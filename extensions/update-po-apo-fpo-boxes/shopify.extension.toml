api_version = "2026-04"

[[extensions]]
name = "t:name"
handle = "update-po-apo-fpo-boxes"
type = "function"
uid = "a9d41362-5e87-6cbd-b49c-bd537895210068cbfcf4"
description = "t:description"

  [[extensions.targeting]]
  target = "cart.delivery-options.transform.run"
  input_query = "src/cart_delivery_options_transform_run.graphql"
  export = "cart-delivery-options-transform-run"

  [extensions.build]
  command = ""
  path = "dist/function.wasm"

  # Points the admin "Add delivery customization" flow at the app's management
  # page. The function takes no config, so create and details share one route.
  [extensions.ui.paths]
  create = "/app/po-box-delivery"
  details = "/app/po-box-delivery"
